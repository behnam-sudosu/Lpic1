# dd

```bash
dd if=/root/sshd_config of=/var/log/sshlog.txt
dd if=/dev/sdb of=/dev/sdc
dd if=/dev/zero of=/root/zerofile
dd if=/dev/zero of=/root/zerofile bs=1M count=100 ===>> make file 100MB
dd if=/dev/sd2 of=/tmp/sda2.img
dd if=/dev/zero of=zerofile bs=1M count=10
sudo dd if=/centos7 of=/dev/sdb bs=4M status=progress oflag=sync ===>> make flash boot
sync ===>> check everything is OK
dd if=/dev/zero of=/root/zerofile ===>> breake your hard disk
```