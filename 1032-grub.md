# grub

```bash
bios ===>> basic input output service
Bios ===>> Mbr ===>> bootloader
post ===>> power on self test
chain loader
bootloading ===>> one bootloader give to another bootloader
    lilo ===>> linux loader
    /etc/lilo.conf
    /usr/sbin/liloconfig ===>> cammand install lilo
```

---

### grub legacy

```bash
grub ===>> legacy grub or grub version 0.9 or 1
/boot/grub/menu.lst
/boot/grub/grub.conf
grub-install /dev/sda1 ===>> install grub
grub-install '(sda1)' ===>> install grub
```

---

### grub2
```bash
/etc/grub/grub.cfg
grub-install /dev/sda1 ===>> install grub
grub2-install /dev/sda1 ===>> install grub
grub-mkconfig ===>> generate grub and know how many os you have
grub-mkcinfig -o (direction) ===>> save it to file
grub-mkconfig > /boot/grub.cfg
update-grub ===>> reconfigure grub
```
```bash
hint: /dev/sda ===>> write in mbr partion
hint: /dev/sda1 ===>> write in disk 1
```
```bash
kernel
initrd
    initramfs
init
shell
```

### command and directory

```bash
dmesg ===>> show all log in system, kernel log

#kernel ring buffer
cat /var/log/dmesg

cat /var/log/messages ===>> centos, show all, program log
cat /var/log/syslog ===>> ubuntu, show all, program log
```
```bash
respawn ===>> if it's go down you bring it up
wait
once

pstree ===>> show your process and dependency
```

### change and edit grub directory

```bash
/etc/default/grub
/etc/default/grub.d
/etc/grub.d
/boot/grub/grub.conf
```